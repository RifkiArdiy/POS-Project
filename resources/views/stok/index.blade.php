{{-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stok</title>
</head>
<body>
    <h1>Data Stok Pengguna</h1>
    <table border="1" cellpadding="2" cellspacing="0">
        <tr>
            <th>ID</th>
            <th>Kode Barang</th>
            <th>Nama User</th>
            <th>Supplier</th>
            <th>Tanggal Masuk</th>
            <th>Jumlah</th>
        </tr>
        @foreach ($data as $item)
        <tr>
            <td>{{ $item->stok_id }}</td>
            <td>{{ $item->barang_id }}</td>
            <td>{{ $item->user_id }}</td>
            <td>{{ $item->supplier_id }}</td>
            <td>{{ $item->stok_tanggal_masuk }}</td>
            <td>{{ $item->stok_jumlah }}</td>
        </tr>
        @endforeach
    </table>
</body>
</html> --}}

